services:
  backend:
    build: ./backend
    expose:
      - "8000"
    volumes:
      - ./backend/data:/app/data

  frontend:
    build: .
    ports:
      - "5173:80"
    depends_on:
      - backend
